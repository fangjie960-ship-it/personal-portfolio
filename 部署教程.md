# 个人作品集网站部署教程

## 部署方式：GitHub Pages（推荐）

### 步骤 1：创建 GitHub 仓库

1. 打开 [GitHub](https://github.com) 并登录
2. 点击右上角 **+** 号 → **New repository**
3. 填写信息：
   - **Repository name**: `个人作品集网站`（或英文如 `portfolio`）
   - **Description**: 可选，填写网站描述
   - **Public**: 选中（免费部署必须公开）
   - **Add a README**: 不要勾选
4. 点击 **Create repository**

---

### 步骤 2：上传项目代码

#### 方式 A：命令行（推荐）

在项目根目录打开命令行，依次执行：

```bash
# 1. 初始化 Git
 git init

# 2. 添加所有文件
 git add .

# 3. 提交更改
 git commit -m "Initial commit"

# 4. 关联远程仓库（将下面的 URL 替换为你的仓库地址）
 git remote add origin https://github.com/你的用户名/个人作品集网站.git

# 5. 推送到 GitHub
 git push -u origin main
```

> 如果 `main` 不行，试试 `master`: `git push -u origin master`

#### 方式 B：拖拽上传

1. 将项目文件夹压缩为 ZIP 文件
2. 在 GitHub 仓库页面点击 **uploading an existing file**
3. 拖拽或选择 ZIP 文件解压后的所有文件
4. 点击 **Commit changes**

---

### 步骤 3：配置自动部署（GitHub Actions）

项目已预配置好自动部署，只需开启 GitHub Pages：

1. 打开 GitHub 仓库页面
2. 点击 **Settings** 选项卡
3. 左侧菜单选择 **Pages**
4. **Source** 选择 **GitHub Actions**
5. 系统会自动检测并配置（因为项目中已包含 `.github/workflows/deploy.yml`）

---

### 步骤 4：等待部署完成

1. 点击仓库的 **Actions** 选项卡
2. 等待工作流运行（显示绿色勾勾 ✓ 表示成功）
3. 部署完成后，在 **Settings → Pages** 查看网站地址
4. 地址格式：`https://你的用户名.github.io/个人作品集网站/`

---

### 步骤 5：后续更新网站

每次修改代码后，执行：

```bash
git add .
git commit -m "更新描述"
git push
```

GitHub 会自动重新部署，约 1-2 分钟后即可看到更新。

---

## 常见问题

### 1. 部署后页面空白？

检查 `vite.config.ts` 中的 `base` 路径是否与仓库名一致：
```typescript
base: '/个人作品集网站/'
```

### 2. 图片不显示？

确保图片放在 `public` 文件夹中，引用时使用相对路径：
```html
<img src="/图片名.jpg" />
```

### 3. 如何绑定自定义域名？

1. 在 `public` 文件夹添加 `CNAME` 文件，内容为域名（如 `www.example.com`）
2. 在域名服务商添加 DNS 记录：
   - 类型: CNAME
   - 主机: www
   - 值: `你的用户名.github.io`
3. 在 GitHub Pages 设置中输入域名并保存

---

## 其他部署方式（可选）

### Netlify（更简单）

1. 打开 [Netlify](https://www.netlify.com)
2. 用 GitHub 账号登录
3. 点击 **Add new site → Import an existing project**
4. 选择 GitHub 仓库
5. 点击 **Deploy site**

### Vercel

1. 打开 [Vercel](https://vercel.com)
2. 用 GitHub 登录
3. 点击 **Add New Project**
4. 选择仓库，点击 **Import**
5. 点击 **Deploy**

---

## 需要帮助？

如果遇到问题：
1. 检查 Actions 页面是否有错误日志
2. 确认 `package.json` 中的构建命令是 `"build": "tsc && vite build"`
3. 确保所有依赖已安装：`npm install`
4. 本地测试构建：`npm run build`，查看 `dist` 文件夹

祝你部署顺利！🎉
